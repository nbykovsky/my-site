language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm test
  - npm run build

deploy:
    provider: gcs
    access_key_id: "$private_key_id"
    secret_access_key: "$private_key"
    bucket: "dev.bykovsky.com"
    skip_cleanup: true
    local-dir: build