language: node_js

node_js:
    - node
    - 'lts/*'

services:
    - docker

script:
    - mkdir tmp; echo "$private_key" >> tmp/key
    - docker build --build-arg PRIVATE_KEY_ID=${private_key_id} --build-arg PRIVATE_KEY="${private_key}" .